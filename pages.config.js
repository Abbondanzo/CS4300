/**
 * Each of these listed items represent a folder in the src/ directory. The
 * folder must contain the following items at top-level:
 * - index.html
 * - index.ts
 */
const assignments = ["assignment1", "assignment2", "assignment3"];

module.exports = { assignments };
